services:
  - type: web
    name: monggeul
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: DEPLOY_BASE
        value: "/"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
